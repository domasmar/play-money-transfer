
POST        /account                            @controllers.Controller.createAccount()
GET         /account/:accountId/transfer        @controllers.Controller.getTransfers(accountId: UUID)

POST        /transfer                           @controllers.Controller.createTransfer()
